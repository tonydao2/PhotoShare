<!DOCTYPE html>

<head>
    <title>Delete an album</title>
</head>

{% if message %}

<h4>{{message}}</h4>

{% endif %}

<body>
    <h2>Delete an Album</h2>
    {% for album in albums %}
    <form method="post" action="/DeleteAlbum">
        <input type="radio" name="albumId" value ={{album[1]}}  /><br />
        <label for="albumId">{{album[0]}}</label><br />
    {% endfor %}

    <input type="submit" value="Delete Album (Will ddelete all photos in album)"/>
    </form>
</body>